# Copy to .env.local for local development, .env.production for production builds.
# Vite exposes only variables prefixed with VITE_ to the browser bundle.

# Base URL for all API calls (proxied by Vite in dev, by nginx in production)
VITE_API_BASE_URL=/api
